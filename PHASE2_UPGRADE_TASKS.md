# 第二阶段升级任务拆分

本文档用于新开 Codex 子对话时复制使用。每个子任务尽量单独完成、单独验证，避免一次改动过大导致功能互相影响。

## 当前版本基线

- 主程序文件：`invoice_compiler.py`
- 打包配置：`InvoiceCompiler.spec`
- 已支持输入：PDF、OFD、图片
- 已支持输出：带页眉的汇编 PDF、打印入口、实时预览、页面删除、页面拖拽排序
- 页眉规则：
  - 横版页面使用顶部页眉
  - 接近竖版 A4 的 PDF / 图片不旋转，使用右侧长边页眉
  - “第 页/共 张”中的“共 张”默认按所有页面的原始凭证张数求和，但仍允许手动修改

## 子任务 1：调整按钮逻辑

目标：让常用操作更顺手，减少误点和重复点击。

建议范围：

- 梳理左侧操作按钮和顶部按钮的启用/禁用状态。
- 未选中页面时，“修改选中”“删除选中”等按钮应给出清晰提示或保持不可用。
- 添加图片时继续保留“新开一页 / 添加到选中页”的选择逻辑。
- 生成 PDF、打印前统一检查是否已有页面。

验收标准：

- 空列表状态下不会误生成空 PDF。
- 选中 PDF / OFD / 图片页后，都能进入正确的修改弹窗。
- 删除、拖拽排序、生成 PDF 后，顶部统计和预览同步刷新。

## 子任务 2：调整页面布局

目标：优化现有界面空间分配，让列表、预览和操作区更清晰。

建议范围：

- 调整顶部统计卡片宽度、间距和显示层级。
- 调整中间页面列表和右侧实时预览的比例。
- 优化窗口较小时的显示效果，避免控件重叠。
- 保持现有简约风格，不引入复杂主题或额外依赖。

验收标准：

- 主窗口打开后，核心按钮、列表和预览区无需拖动即可辨认。
- 预览区比当前版本更易看清排版。
- 不影响现有 PDF / OFD / 图片添加、修改、删除、排序和导出功能。

## 子任务 3：新增图像识别功能

目标：对图片、扫描 PDF、OFD 转换后的页面进行更准确的自动识别，尽量识别出报销金额和原始凭证张数。

建议范围：

- 先调研本地可打包的 OCR 方案，优先考虑离线、可随 exe 分发的方案。
- 识别金额时优先匹配“价税合计”“合计金额”“小写”“金额”等常见字段。
- 对识别结果保留人工修改入口，不强制覆盖用户手动输入。
- 对识别失败的文件给出温和提示，仍允许手动录入。

验收标准：

- 常见电子发票截图能自动填入报销金额。
- 多张图片同页时，原始凭证张数仍按该页图片数量默认计算，并允许手动修改。
- OCR 依赖在打包 exe 后仍能正常工作，或文档明确说明需要额外安装的组件。

## 建议执行顺序

1. 先做按钮逻辑，降低后续测试成本。
2. 再做页面布局，避免布局调整影响按钮状态判断。
3. 最后做图像识别，因为它涉及依赖、打包和准确率测试，风险最高。

## 每个子任务开始前建议复制给 Codex 的提示

请基于当前 `invoice_compiler.py` 做小范围修改，不要重构无关代码。修改前先说明计划，修改后运行语法检查，并说明需要我手动验证的界面流程。
